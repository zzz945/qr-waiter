export const setCurOrder = ({ dispatch }, order) => dispatch('SET_CUR_ORDER', order)
export const setOrderList = ({ dispatch }, list) => dispatch('SET_ORDER_LIST', list)
export const addOrder = ({ dispatch }, order) => dispatch('ADD_ORDER', order)
export const removeOrder = ({ dispatch }, order) => dispatch('REMOVE_ORDER', order)
